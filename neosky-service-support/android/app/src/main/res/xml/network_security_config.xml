<?xml version="1.0" encoding="utf-8"?>
<!-- Allows cleartext HTTP only to the Android emulator's host loopback, so local
     debug builds can reach a backend running on the host machine at 10.0.2.2:8000.
     Release builds must use HTTPS (see API_SPEC.md security requirements). -->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
